#!/usr/bin/env bash
. "$(dirname -- "$0")/_/husky.sh"

# Run lint-staged
npx lint-staged || exit 1

# Run tests
npm test || exit 1

# Note: Remove the commitlint check from pre-commit
# commitlint should be in commit-msg hook instead
